// https://storybook.js.org/docs/react/configure/theming
import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: '@caripizza/ui',
  brandUrl: '/',
  brandTarget: '_self',
});