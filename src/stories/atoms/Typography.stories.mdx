import { Meta, Typeset, Source, Canvas } from '@storybook/addon-docs';
import { typography, fontSizesRem, fontSizesPx } from '../../atoms';

<Meta title="Atoms/Typography" />

export const allFontSizes = Object.values({ ...fontSizesPx });
export const loremIpsum = 'Lorem ipsum dolor sit amet';

# Typography
The default library font is Arimo, available in the global stylesheet along with other CSS Vars for fonts:

**Family:** [Arimo](https://fonts.google.com/share?selection.family=Arimo:wght@400;500;600)

<Source
  language='js'
  format
  code={`
--font-sans // ${typography.fontFamily.sans}
--font-mono // ${typography.fontFamily.mono}
`}
/>

**Sizes:**

<Source
  language='jsx'
  format
  code={
`${Object.keys(fontSizesRem).map((el) => {
    return `
    --font-size-${el} // ${fontSizesRem[el]}rem (${fontSizesPx[el]}px)`
  })}`
}/>

**Weights:**

<Source
  language='js'
  format
  code={`
--font-weight-regular // ${typography.fontWeight.regular}
--font-weight-medium // ${typography.fontWeight.medium}
--font-weight-semibold // ${typography.fontWeight.semibold}
`}
/>

<Canvas>
  <div
    style={{
      display: 'grid',
      gridColumnGap: '0.5em',
      gridTemplateAreas:
        `"rg md sb"
        "rg md sb"
        "rg md sb"`
    }}
  >
    <style type="text/css">
      {`.regular {
        grid-area: rg;
        width: 300px;
        font-weight: var(--font-weight-regular);
      }
      .medium {
        grid-area: md;
        width: 300px;
        font-weight: var(--font-weight-medium);
      }
      .semibold {
        grid-area: sb;
        width: 300px;
        font-weight: var(--font-weight-semibold);
      }`}
    </style>
    <div className="regular rg">
      <span>400 (regular)</span>
      <div>
        <Typeset fontSizes={allFontSizes}
          sampleText={loremIpsum}
          fontFamily={typography.fontFamily.sans}
        />
        <Typeset fontSizes={allFontSizes}
          sampleText={loremIpsum}
          fontFamily={typography.fontFamily.mono}
        />
      </div>
    </div>
    <div className="medium md">
      <span>500 (medium)</span>
      <div>
        <Typeset fontSizes={allFontSizes}
          sampleText={loremIpsum}
          fontFamily={typography.fontFamily.sans}
        />
        <Typeset fontSizes={allFontSizes}
          sampleText={loremIpsum}
          fontFamily={typography.fontFamily.mono}
        />
      </div>
    </div>
    <div className="semibold sb">
      <span>600 (semibold)</span>
      <div>
        <Typeset fontSizes={allFontSizes}
          sampleText={loremIpsum}
          fontFamily={typography.fontFamily.sans}
        />
        <Typeset fontSizes={allFontSizes}
          sampleText={loremIpsum}
          fontFamily={typography.fontFamily.mono}
        />
      </div>
    </div>
  </div>
</Canvas>
